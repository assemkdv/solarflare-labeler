[metadata]
name = solarflare-labeler
version = 0.1.0
description = A Python package for labeling solar flare prediction datasets
author = Assem Kadirova
python_requires = >=3.10

[options]
package_dir =
    = src
packages = find:

[options.packages.find]
where = src